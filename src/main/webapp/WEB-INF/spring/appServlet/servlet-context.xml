<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc-3.1.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context-3.1.xsd">

	<!-- spring mvc 컴포넌트 활성화 RquestMapping 사용 -->
	<mvc:annotation-driven />
	
	
	<!-- 정적 파일 관리를 위한 설정 -->
	<mvc:resources location="/static/" mapping="/**"/>
	
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/view/"/>
		<property name="suffix" value=".jsp" />
	</bean>
	
		
	<!-- 로그인시 사용되는 인터셉터 bean 등록 -->
	<bean id="loginInterceptor" class="com.goCamping.interceptor.LoginInterceptor" />
	<!-- 로그인 하지 않은 경우 또는 세션에 저장된 유저가 현재 요청받은 유저와 일치한지 체크하여 권한이 필요한 작업에 사용할 인터셉터 -->
	<bean id="authLoginInterceptor" class="com.goCamping.interceptor.AuthInterceptor" />
	<!-- 로그인 하지 않은 경우,  -->
	<bean id="notLoginInterceptor" class="com.goCamping.interceptor.NotLoginInterceptor" />
	
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/member/login"/>
			<ref bean="loginInterceptor" />
		</mvc:interceptor>
		
		<mvc:interceptor>
			<mvc:mapping path="/member/edit/**"/>
			<mvc:mapping path="/member/logout/**"/>
			<ref bean="authLoginInterceptor"/>
		</mvc:interceptor>
		
<!-- 		<mvc:interceptor> -->
<!-- 			<mvc:mapping path="/member/check/**"/> -->
<!-- 			<ref bean="notLoginInterceptor" /> -->
<!-- 		</mvc:interceptor> -->
		
	</mvc:interceptors>
	
	<context:component-scan base-package="com.goCamping.controller" />
</beans>